<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Verify OTP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .login-container {
            padding: 30px;
            border: 1px solid #dee2e6;
            border-radius: 10px;
            background-color: white;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }
    </style>
</head>
<body>
<div class="login-container">
    <header>
        <div class="text-center mb-3">
            <a href="/"><img src="https://images.unsplash.com/photo-1763929272741-56e50999033f?w=500" height="60" width="60"></a>
        </div>
    </header>

    <main>
        <% if (typeof error !== 'undefined' && error) { %>
            <script>alert("<%= error %>");</script>
        <% } %>

        <div class="mb-4 p-3 border rounded">
            <h5 class="text-center mb-2">Verify OTP</h5>
            <p class="text-center text-muted small">
                Enter the 6-digit OTP sent to your email
            </p>

            <form method="POST" action="/verify-otp">
                <div class="mb-3">
                    <input type="text" class="form-control text-center" name="otp" placeholder="Enter OTP" maxlength="6" required>
                </div>

                <div class="d-flex justify-content-center">
                    <button type="submit" class="btn btn-primary mb-3">Verify</button>
                </div>
            </form>
        </div>
    </main>
</div>
</body>
</html>
